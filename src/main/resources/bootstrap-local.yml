spring:
  application:
    name: "sky-project-2"
  profiles:
    active: "local"
  cloud:
    config:
      uri: "http://localhost:18888"
      
management:
  endpoints:
    web:
      exposure:
        include: refresh